/**
 * @format
 */

import {AppRegistry} from 'react-native';
import WeatherScreen from './src/pages';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WeatherScreen);
